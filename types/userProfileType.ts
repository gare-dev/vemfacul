export type UserProfileType = {
    nome: string
    username: string
    foto: string
    header: any
    descricao: string
    followers_number: string
    following_number: string
    posts_number: string
    vestibulares: string[]
    materias_lecionadas: string[]
    nivel: string
    acertosuser: number
    verified_account: boolean
}

export type ProfileType = {
    nome: string
}